{
  "name": "delete_file",
  "description": "Delete one file from a project. Supports nested paths (e.g. \"codex-product/index.html\"). Project optional; defaults to the active project.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project": {
        "type": "string",
        "description": "Project id (UUID) or name substring. Optional; defaults to the active project (expires after ~5 minutes of no Open Design activity)."
      },
      "path": {
        "type": "string",
        "description": "Project-relative path of the file to delete."
      }
    },
    "required": [
      "path"
    ],
    "additionalProperties": false
  }
}